;;; post.el --- this is file is evaled by lens.el

;; (if (file-exists-p "/usr/bin/wkhtmltopdf")
;; 	(shell-command "wkhtmltopdf ../resume.htm ../resume.pdf")
;;   (message "install wkhtmltopdf for pdf creation"))
